# Data

Datasets and machine-readable inputs and outputs that support the papers in [`papers/`](../papers).

Group files by paper or topic in clearly named subfolders, and include a short note on provenance and format alongside each dataset. Keep large binary data out of git history where practical — link to an external store if a file is too large to archive here comfortably.
