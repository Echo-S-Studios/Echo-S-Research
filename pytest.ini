[pytest]
# Tests live under tests/, mirroring papers/ one folder per paper.
testpaths = tests
# importlib mode lets same-named test files (e.g. test_constants.py) coexist
# across per-paper folders without __init__.py or basename collisions.
addopts = --import-mode=importlib
xfail_strict = false
