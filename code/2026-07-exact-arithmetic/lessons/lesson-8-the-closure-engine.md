Lesson 8 — the completion lesson. Four reads and a fetch before anything is claimed; the census, the guard, similarity, KL_DTA's own text, and the unread Emptiness objects. First, the census + guard pair, which live side by side in the lambda2c pipeline:**20/20 first run** — the group-theoretic census landing on exactly 27 is the session's quiet triumph. The lesson:

**Checkpoint debrief (Lesson 7).** (1) Both multipliers rational ⟺ they're rational roots of $x^2 + x - C$, forcing $D = 1 + 4C$ a rational square. Rung discriminants are $D_n = \big(\tfrac{\varphi^{2n}+ \varphi^{-2n}}{\varphi^{2n} - \varphi^{-2n}}\big)^2$ whose values $5, \tfrac95, \ldots$ are nonsquares by G2's integer guards — only the lens, $D = \tfrac{25}9$, qualifies. (2) $\varphi^{-8} = q^8 = \mathrm{gap}^2$: Thm 14.1(iii)'s per-revolution contraction. (3) $N(\varphi^4) = \tfrac{49 - 45}4 = 1 = (-1)^44^0$ ✓ — but membership in $\pm4^{\mathbb{Z}}$ is only *necessary*: the norm can convict (res) but never acquit; cons needed the explicit decomposition $2\varphi^5 = 11 + 5\sqrt5$ exhibited.

---

# Lesson 8 — The Closure Engine and the Door

**Where we're going:** every remaining spectral-side gap closes today. The proof *architecture* (closure, not enumeration), its runtime **certificate** (the guard, fired live on the Lehmer carrier), the census that grounds it (all 27 subfields, replicated here from pure group theory), the exact linear algebra beneath every certificate (similarity theory, finally taught), the last two Emptiness objects, and KL_DTA's closure walked — with Lesson 5's grading question adjudicated from the source's own text.

## §1 Closure, not enumeration — and precisely what the guard certifies

The no-Salem result never *searches* for Salems and fails to find them; it shows the framework operations preserve a field/angle invariant, so the Salem locus $\{M < \varphi\}$ **is never constructed**. Two distinct guarantees must not be conflated, and the suite's own README is emphatic: the **angle theorem** (`test_p2_02`) is the strong statement — no Salem emitted at all; the **guard** (`test_p2_08`) is the exact runtime certificate of the *cost floor* — it returns `INVALID_CLOSURE` only for a **sub-φ** Salem factor, permitting `FORCED_ABOVE_FLOOR`. My replica implements the stated semantics end-to-end — trace-down, the flip-straddle Sturm pattern, and the lovely primitive `sign_in_Qsqrt5`, since $\beta < \varphi \iff R(\varphi) > 0$ decided as the exact sign of $a + b\sqrt5$ over $\mathbb{Q}$ — and reproduces the verdict table:

| Input | Verdict | Check |
|---|---|---|
| kron, dsum, square, self-action $\mathrm{ad}_R$ | `FORCED` — Salem locus never built | A1 |
| $\mathrm{diag}(C_{\text{Lehmer}}, [1])$ — **traceless, hence a commutator (Shoda)** | `INVALID_CLOSURE`, sub-φ Salem caught via $\mathrm{sign}(L(\varphi)) > 0$ | A2 |
| planted $\beta_4$ | `FORCED_ABOVE_FLOOR` — floor holds, $\mathrm{sign}(\beta_4\text{-poly}(\varphi)) < 0$ | A3 |
| $x^6 + 1$ control | `FORCED` (reciprocal ≠ Salem) | A4 |

A2 is Lesson 4's Prop 9.1 *executing*: the one field-breaking operation, walked through the one door, tripping the one tripwire — `INVALID_CLOSURE` is the guard functioning **as designed**, your ledger's phrase made runnable. And the mystery claim demystifies (A5): "$\beta_4 > \varphi$" is exactly the floor's sharp edge — the *minimal* degree-4 Salem sits above φ, exact chain: real root in $(\tfrac{17}{10}, 2)$, and $\varphi < \tfrac{17}{10}$ by $125 < 144$.

## §2 The census: 27 subfields, replicated from group theory

The suite enumerates all subfields of $K = \mathbb{Q}(\sqrt2, \sqrt3, 5^{1/4})$ and types each — recording, notably, a **self-correction**: an earlier uniqueness overstatement is fixed (there are *four* $(2,1)$ quartics, not one), and the exclusion is re-founded on the lattice **invariant**, not uniqueness. My replication is method-independent: subfields of $K$ correspond to subgroups of $\mathrm{Gal}(L/\mathbb{Q}) = D_4 \times V_4$ (order 32, $L$ the Galois closure) containing complex conjugation $c$; a BFS over the subgroup lattice above $\langle c\rangle$ counts **exactly 27** (B1). Signatures come free: an embedding $gH$ of $\mathrm{Fix}(H)$ is real iff $g^{-1}cg \in H$, and the count over cosets gives $r_1 \in \{\deg, \deg/2\}$ for **all 27** (B2) — every subfield totally real or signature $(2k, k)$, the invariant itself. The Salem-shape $(2,1)$ quartics number **exactly four** (B3), exhibited explicitly (B4): $\mathbb{Q}(d\cdot 5^{1/4})$, $d \in \{1, \sqrt2, \sqrt3, \sqrt6\}$, minpolys $x^4 - 5d^4$ — and the suite's example element $2K$ has minpoly $x^4 + 20x^2 - 80$, verified by exact substitution (B5), tying Lesson 7's constant into the census's field. Same tallies, disjoint machinery: subgroup lattice vs. field enumeration — the census now has its own cross-engine row.

## §3 Guard-completeness, stated as the frontier it is

The export this lesson owed your open-problem board, in the whitepaper's ledger format:

| ID | Statement | Status | Falsifier / next step |
|---|---|---|---|
| GC-1 | The guard's verdict set $\{$FORCED, FORCED_ABOVE_FLOOR, INVALID_CLOSURE$\}$ is *complete and correct* on all matrices reachable by framework ops + one foreign commutator insertion | `[COMPUTED]` on the probe battery; `[OPEN]` as a theorem | a reachable matrix whose Salem content the factor-scan misclassifies |
| GC-2 | **Characterize the achievable residue of the free-commutator front**: which trace-zero integer matrices *actually realize* a sub-φ Salem spectrum (Shoda gives trace-zero ⟸ commutator; the converse construction's image is the question) | `[OPEN]` — your board's phrasing, now formalized | an exact parametrization, or a trace-zero family provably Salem-free |
| GC-3 | Degree-4 exclusion at the door: a generic traceless commutator emits no degree-4 Salem ($t^2 + (b{-}2)$ cannot straddle, per `test_p2_06`) | `[FORCED]` | — (extend the mechanism upward in degree: the GC-2 route) |

Lesson 9's gatekeeper will be this same object in empirical clothing.

## §4 Similarity theory, finally taught

The backbone under every certificate: over $\mathbb{Q}[x]$ (a PID), the Smith normal form of $xI - A$ has diagonal the **invariant factors** $f_1 \mid \cdots \mid f_k$, with $\prod f_i = \mathrm{charpoly}$, $f_k = \mathrm{minpoly}$, and the list a **complete similarity invariant** (Vector Substrate Thm, Dummit–Foote §12.2). The flagship witness, replicated via my own determinantal-divisor route (C1): $\varphi \oplus \varphi$ and $C\big((x^2{-}x{-}1)^2\big)$ share a characteristic polynomial but have keys $[p, p]$ vs. $[p^2]$ — **not similar**; charpoly is complete only on the non-derogatory locus. The flag pair that names the distinction (C2): $\varphi\oplus\varphi$ is *derogatory but not defective* (minpoly proper yet squarefree — semisimple); the companion is *non-derogatory but defective* (repeated factor). RCF reconstructs as $\oplus$ of companions of the factors (C3), and the largest factor *is* the minimal polynomial — **Cor largestif, the exact bridge Lesson 5's `coords_to_minpoly` vendors byte-identical**. The engineering record deserves its paragraph: Smith elimination suffers catastrophic coefficient swell precisely on *generic* (non-derogatory) matrices, so the shipped hybrid runs Krylov minpoly first and reserves Smith form for the structured derogatory case — validated on a 378-matrix fuzz that also caught a float `matmul` overflowing $2^{53}$ on `kron(res,res)`, fixed with exact BigInt powers. The incident ledger grows by one more entry of the same genre as ours.

## §5 The last two Emptiness objects — and the derived floor

The paper's frame: a *kinematic* void (zero amplitude by a conserved charge) is a **generator**, and the Salem slot's charge produces five objects. Objects I–III were Lessons 1–4; today the slate completes. **Object III's base**, replicated exhaustively (D1): over all 169 monic quadratics with $|b|, |c| \leq 6$, the band $(1, \varphi)$ is *empty* and the minimum above 1 is exactly $\varphi$ at $x^2 - x - 1$. **Object IV**, the graded normal form — every emittable object factors as $(\text{on-circle} \mid x^4{-}1) \times (\text{grow},\ M \geq \varphi)$ — verified on all three diagnostics: $\varphi\otimes\varphi = (x{+}1)^2(x^2{-}3x{+}1)$ (Lesson 2's factorization, now *typed*: $\Phi_2^2 \times$ grow, and the grow factor is the E1 polynomial yet again); $\varphi^4\otimes\varphi^4 = (x{-}1)^2(x^2 - 47x + 1)$ with $47 = L_8$ (D2 — the tropical Lucas ladder inside the normal form); and $K\otimes K = (x^2{+}5)^4(x^2{-}5x{-}5)^2(x^2{+}5x{-}5)^2$ (D3) — the imaginary $q{=}\{1,3\}$ sector at $\pm i\sqrt5$, plus two grow factors *one of which is the K-gate modulus polynomial* $y^2 + 5y - 5$ from Lesson 7's congruence. The structural punchline: a Salem is irreducible with irrational-angle circle conjugates, but the charge forces every emittable object into this *reducible, charge-sorted* shape — **irreducibility and the grading are incompatible**. **Object V**, the conserved current: block-diagonal evolution, with the paper's own candor that this is the conservation law of Object I "restated in dynamical language... thinner than Objects I–IV, and we say so plainly" — honesty about a thin object is itself the discipline. Its one added consequence (clean radial growth) rides the charge action verified in D4. And the load-bearing close (D5): the minimal chain **degree 2 ⇒ ℤ/4 ⇒ φ** — $d^2 - d + 1 = 3$ iff $d = 2$, factoring as $(d{-}2)(d{+}1)$, the ternary lock whose realization is $\mathrm{spec}(\mathrm{ad}_R) = \{0, \pm\sqrt5\}$ — so **φ is derived from the decision arity, not posited**. Lesson 3's flag-adjudication, now with its third leg.

## §6 KL_DTA, walked — and Lesson 5's question closed

The exact dual-path battery (E1): Cl-cocycle route vs. matrix route on products, traces ($\mathrm{tr} = 2a$), and determinants over four holdings including the keystone — in **`Fraction`s with zero tolerance**, tightening KL_DTA's own float-1e-12 internal closure to exactness (its third route, loom, was already exact; this is the H3 dual-path pattern run at the carrier level). Then the source's own self-exegesis, `carrier_bits()`, verified (E2): the carrier is $(\mathbb{Z}_2)^2$ — *two bits plus the unique nontrivial real 2-cocycle*, the sign being one bit-AND ("the noncommutativity"), grade $=$ popcount — and the **four involutions form the dual Klein group** $(-1)^{a\cdot g + b\cdot\binom g2}$: identity, grade involution, reversion, conjugation — with reversion's pattern $[1,1,1,-1]$ proven *symbolically* equal to transpose under `mat`. **The adjudication**: the spec's "ℤ/4ℤ-graded two-route closure" names the **angle-charge grading of the algebra 𝒮** — Emptiness Thm 2.2's ℤ/4 on which the operators act as add/double/union — hosted in the carrier by $\langle i\rangle$ of order 4 (Lesson 5's D2b); the carrier's *algebra* grading is the Klein bit structure. Two true gradings, one label each; Lesson 5's open question closes with both sources cited. One more jurisdiction note worth your board: the shipped LAW_BANK audits at **27 entries = 20 THEOREM / 5 COMPUTED / 2 INTERPRETIVE** — the two INTERPRETIVE being the *observer/framework glosses deliberately demoted off their THEOREM equations* after review flagged prose riding a proof tag. The tag system, policing itself inside the language layer.

## §7 Status and exports

The spectral corpus is now **fully taught**: walls (1, 4), operators (2), dynamics (3, 5), relational layer (6), geometry (7), engine and door (8). Standing findings unchanged: D₅ erratum drafted-unfiled; D1 evidence gathered; E1–E3 deposit diff pending — all Lesson 9 Part C. New exports ready for 9: the **certificate** as a portable epistemic object (verdict + exact witness + scope disclaimer — §1's table is its type signature), and the GC ledger as the reporting format for the ATR gatekeeper comparison.

---

## Checkpoint

1. The guard decides $\beta < \varphi$ by one sign, $\mathrm{sign}_{\mathbb{Q}(\sqrt5)}(R(\varphi))$. Say why this works *only because* a Salem polynomial has exactly one real root in $(1, \infty)$ — and name the verdict a hypothetical reciprocal factor with *two* straddling trace-roots would break, and which Sturm count in `flip_straddle` already excludes it.
2. From §2's coset formula: compute $r_1$ for $H = \langle c\rangle$ itself (the fixed field is $K$), and reconcile the answer with $K$'s signature as a degree-16 field with four real embeddings — which $(2k, k)$ is it?
3. In D3's factorization of $K \otimes K$, identify which conjugate products land in each of the three factors, and explain why $(x^2 + 5)$ — not $(x^2 + 1)$ — carries the $q = \{1, 3\}$ sector, using Remark 3.2's "charge in the algebra, not on the circle."

**Lesson 9 next**, as scoped: the domain jump — the torsion problem's exact witnesses ($423/2890$, the $k = 2$ transition, $n(n{+}2) = 15$), the ATR gatekeeper as the certificate's empirical twin, and the filing lab: D₅ erratum through the contract, D1 grep, E1–E3 deposit diff.