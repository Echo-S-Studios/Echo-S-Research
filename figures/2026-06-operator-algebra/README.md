No figures appear in papers/2026-06-operator-algebra/operator-algebra-whitepaper.tex.

The paper contains no `\begin{tikzpicture}`, `\begin{figure}`, or
`\includegraphics` environments (it presents two plain `tabular` tables — the
self-action rate table in Sec. 6 and the epistemic ledger in Sec. 9 — neither
of which is a figure). Nothing to reconstruct.
