# Figures

Figures, plots, and images used in the papers.

Prefer vector formats (`.pdf`, `.svg`) for line art and high-resolution raster (`.png`) for everything else. Where a figure is generated by a script, keep that script in [`code/`](../code) so the figure can be regenerated.
